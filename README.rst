identic | Find duplicate files
##############################

Usage
-----

.. code-block:: none

    # identic --progress --out-file identic.json <dir>
    # identic --duplicates-only --in-file identic.json --report

And without the intermediate file.

.. code-block:: none

    # identic --duplicates-only --progress --report <dir>
